class Solution:
    def longestPalindrome(self, s: str) -> str:
        """
        1. Visualize solution: n-longest palindrome
        2. Brute force - move right index across from n to 0, keeping left constant. Then move left one space and repeat
        3. Should start with 0 and -n. In python, can use the reverse a string with [::-1]
        4. Resetting the longest count
        """

        def find_longest(sub_s,left, right):
            pass

        




        